[Unit]
Wants=network-online.target
After=network-online.target

# Expose the T3 Code server beyond loopback so other devices can pair.
# T3CODE_HOST is read by `t3 serve`; blank/unset means desktop-mode loopback.
[Service]
Environment=T3CODE_HOST=0.0.0.0
